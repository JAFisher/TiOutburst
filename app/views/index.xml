<Alloy>
	<Window class="container" layout="vertical" onOpen="init" >
        <View height="30%">
            <Label id="presentationText" />
        </View>
        <ScrollView layout="vertical" height="70%" backgroundColor="black">
                <Button onClick="pause" > Pause </Button>
                <Button onClick="resume" > Resume </Button>
                <Button onClick="replay" > Replay </Button>
                <Button onClick="speakInFrench" > Change To French </Button>
                <Button onClick="speakInEnglish" > Change To English </Button>
                <Button onClick="availableLanguages" >  Available Languages</Button>
        </ScrollView>

	</Window>
</Alloy>